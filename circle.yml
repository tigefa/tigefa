machine:
  timezone:
    Asia/Jakarta
  ruby:
    version: 2.3.1
  services:
    - redis

test:
  post:
    - bundle exec rake test

#deployment:
#  staging:
#    branch: master
#    heroku:
#      appname: tigefa
