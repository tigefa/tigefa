targets:
  ubuntu-14.04:
    build_dependencies:
      - sqlite3
      - libsqlite3-dev
  ubuntu-16.04:
    build_dependencies:
      - sqlite3
      - libsqlite3-dev
  debian-7:
    build_dependencies:
      - sqlite3
      - libsqlite3-dev
  centos-6:
    build_dependencies:
      - sqlite
      - sqlite-devel
notifications: false
services:
  - redis